spring:
  application:
    name: SpringBackend
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:h2:file:./data/pas-mvp;MODE=PostgreSQL;AUTO_SERVER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true

server:
  port: 8080

pas:
  ai:
    base-url: https://api.deepseek.com
    model: deepseek-chat
    deepseek-api-key: sk-d35654c6c312433ca5b3abcd62796b76
    confidence-threshold: 0.8
  export:
    worker-base-url: http://localhost:8000
    freeze-estimate: 18
    download-root: E:/CodeHome/LogicProjector/FastBackend/outputs
  queue:
    exchange: pas.task.direct
    dead-letter-exchange: pas.dlx.direct
    generation-routing-key: generation.task
    export-routing-key: export.task
    generation-dlq-routing-key: generation.dlq
    export-dlq-routing-key: export.dlq
